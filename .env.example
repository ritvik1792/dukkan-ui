# Spring Boot API (dukkan). Browser calls this origin directly; CORS is enabled on the backend.
# Production: set this to the Cloud Run API URL *before* `docker build` / Cloud Build
# (NEXT_PUBLIC_* is inlined at build time).
NEXT_PUBLIC_API_URL=http://localhost:8080
